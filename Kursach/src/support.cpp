#include "support.h"

cl2::cl2(cl_base* root, cl_base* ptr, std::string&& name)
    : cl_base(root, ptr, std::move(name)) {}

cl3::cl3(cl_base* root, cl_base* ptr, std::string&& name)
    : cl_base(root, ptr, std::move(name)) {}

cl4::cl4(cl_base* root, cl_base* ptr, std::string&& name)
    : cl_base(root, ptr, std::move(name)) {}

cl5::cl5(cl_base* root, cl_base* ptr, std::string&& name)
    : cl_base(root, ptr, std::move(name)) {}

cl6::cl6(cl_base* root, cl_base* ptr, std::string&& name)
    : cl_base(root, ptr, std::move(name)) {}
