#include "support.h"

cl2::cl2(cl_base* ptr, const std::string& name) : cl_base(ptr, name) {}

cl3::cl3(cl_base* ptr, const std::string& name) : cl_base(ptr, name) {}

cl4::cl4(cl_base* ptr, const std::string& name) : cl_base(ptr, name) {}

cl5::cl5(cl_base* ptr, const std::string& name) : cl_base(ptr, name) {}

cl6::cl6(cl_base* ptr, const std::string& name) : cl_base(ptr, name) {}
